# Core pipeline
PyMuPDF>=1.24.0
python-pptx>=0.6.23
markdown>=3.6
weasyprint>=62.0
python-dotenv>=1.0.0

# OCR — text extraction from image-based PDFs (GPU-accelerated)
easyocr>=1.7.0
Pillow>=10.0.0
numpy>=1.24.0

# LLM client (used by vllm, ollama, openai, groq, together backends)
openai>=1.0.0

# Local GPU inference (install on your server with A100)
# pip install vllm
# vllm>=0.6.0

# Cloud-only providers (install only if needed)
# google-generativeai>=0.8.0
# anthropic>=0.30.0
