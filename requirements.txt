# Core pipeline
PyMuPDF>=1.24.0
python-pptx>=0.6.23
markdown>=3.6
weasyprint>=62.0
python-dotenv>=1.0.0
requests>=2.28.0

# OCR — text extraction from image-based PDFs (GPU-accelerated)
easyocr>=1.7.0
Pillow>=10.0.0
numpy>=1.24.0

# ── Optional: cloud-only providers (install only if needed) ──
# pip install google-generativeai   # for gemini provider
# pip install anthropic             # for anthropic provider
